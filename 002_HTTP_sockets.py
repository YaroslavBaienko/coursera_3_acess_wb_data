import socket

# mysock = socket.socket(socket.AF_INET, socket.SOCK_STREAM)
# mysock.connect(('data.pr4e.org', 80))
# cmd = 'GET http://data.pr4e.org/page2.htm HTTP/1.1\n\n'
# mysock.send(cmd.encode('utf-8'))
#
# while True:
#     data = mysock.recv(1024)
#     print(data)
#     if (len(data) < 1):
#         break
#     print(data.decode())
# mysock.close()

print(ord('i'))
